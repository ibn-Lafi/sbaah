# سبعة (SBAAH) — تعليمات دائمة لأي جلسة عمل على هذا المستودع

منصة SaaS عقارية سعودية. البنية: `apps/api` (باكند مركزي v1)، `apps/dashboard`
(لوحة الوسيط/الفريق)، `apps/console` (لوحة مالك المنصة)، `apps/public-site`
(الموقع العام لكل مستأجر)، `packages/shared` (Zod schemas + عميل API + أنواع
مشتركة يستهلكها الجميع من المصدر مباشرة بلا build)، `supabase/migrations`
(ملفات SQL مرقّمة — **تُطبَّق يدويًا من المؤسس عبر SQL Editor في سوبابيس، لا
يوجد CI/CD تلقائي لقاعدة البيانات**). النظام **يعمل فعليًا في بيئة إنتاج حقيقية
على Railway**، وليس مشروعًا تجريبيًا — أي تغيير هنا يمس مستخدمين حقيقيين.

## الانضباط المطلوب في كل مهمة، بلا حاجة لطلبه من جديد كل مرة

### أثناء التنفيذ
- **مراجعة أمنية فعلية، لا شكلية**: RLS على أي جدول/عمود جديد يُلمس من عميل
  غير service-role، تفكير صريح في أي مسار تصعيد صلاحيات جانبي (كما حصل في
  هجرة `0041_users_self_update_email.sql`)، لا أسرار بالنص الصريح في أي ملف
  مُدار بـ git، تحقق من كل مُدخل عبر Zod قبل لمس القاعدة.
- **كود نظيف**: بلا تعليقات تشرح "ماذا" (الكود الواضح يكفي) — فقط "لماذا" عند
  وجود قيد غير بديهي. بلا تكرار — استخرج schema/دالة مشتركة عند تكرار منطق
  (كما حصل مع `emailSchema` بـ`packages/shared`). أسماء واضحة، بلا اختصارات
  غامضة.
- **ترتيب الملفات**: اتبع البنية القائمة فعليًا (`lib/<domain>/`,
  `validation/<domain>.ts`, إلخ) بدل ابتكار بنية جديدة، وبلا ملفات لا حاجة
  حقيقية لها.
- **مراجعة UI/UX لأي تغيير واجهة**: تحقّق بصريًا فعليًا (ليس فقط typecheck) —
  عبر Playwright + خادم mock محلي، على عرض الجوال والكمبيوتر معًا، وتأكد من
  التطابق مع هوية النظام القائمة (الألوان، الأزرار، الخطوط، تباعد العناصر) لا
  إعادة اختراع نمط مختلف.
- **typecheck + lint + build نظيفة** على كل حزمة/تطبيق تأثّر — `packages/shared`
  أولًا (يُستهلك من الجميع)، ثم `apps/*` المتأثرة.

### تقرير ختامي إلزامي عند الانتهاء من أي مهمة (بلا حاجة لطلبه)
1. **ملخص التنفيذ**: ماذا تغيّر فعليًا، وأين (بالملفات/المسارات).
2. **خطوات يدوية مطلوبة من المؤسس، بالتفصيل والترتيب**:
   - أي ملفات SQL جديدة في `supabase/migrations/` يلزم تشغيلها (بالترتيب
     الرقمي، مع أي تحذير قبل التشغيل — مثال: فحص بيانات مكرّرة قبل قيد
     فريد جديد).
   - أي متغيرات بيئة جديدة أو صارت بلا استخدام في Railway (اسم كل خدمة
     متأثرة، القيمة المطلوبة أو طريقة توليدها).
   - أي إعداد خارجي (SNDR، Authentica، StreamPay، إلخ) يلزم التحقق منه أو
     ضبطه يدويًا خارج الكود.
   - أي نشر/إعادة تشغيل مطلوب بعد الدمج.
3. **ما لم يُختبر فعليًا (🔴 صراحة، لا إخفاء)**: أي جزء يحتاج اتصالًا حيًّا
   بسوبابيس/Railway/مزوّد خارجي تعذّر من هذه البيئة — لا ادّعاء تحقق كامل
   لما لم يُختبر فعليًا.
4. **مراجعة UI/UX** (عند وجود تغيير واجهة): وصف مختصر لما تم التحقق منه
   بصريًا فعلًا (تطابق التصميم، الاستجابة على الجوال، أي ملاحظة أو مفاجأة).

استخدم هذا التنسيق دائمًا لأي أمر يُعطى مستقبلًا على هذا المستودع، بلا حاجة
لتذكير جديد في كل مرة.
