# public-site has no authenticated Supabase session (visitors are anonymous).
# It calls `api`'s unauthenticated /public/* endpoints only (PRODUCT_SPEC section 10).
NEXT_PUBLIC_API_URL=http://localhost:3001/v1
