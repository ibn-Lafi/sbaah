'use client';
import{useCallback,useEffect,useState}from'react';import{Button}from'@/components/ui/button';import{Card}from'@/components/ui/card';import{Input}from'@/components/ui/input';import{createPhase,createUnitType,listPhases,listProjectAssets,listUnitTypes,type ProjectPhase,type UnitType}from'@/lib/api/developer-inventory';import type{Asset}from'@sbaah/shared';import{CreateAssetForm}from'@/components/properties/create-asset-form';
export function ProjectInventory({projectId,accessToken,canManage}:{projectId:string;accessToken:string;canManage:boolean}){const[phases,setPhases]=useState<ProjectPhase[]>([]),[types,setTypes]=useState<UnitType[]>([]),[assets,setAssets]=useState<Asset[]>([]),[phaseName,setPhaseName]=useState(''),[typeName,setTypeName]=useState(''),[showAssetForm,setShowAssetForm]=useState(false);const load=useCallback(async()=>{const[p,t,a]=await Promise.all([listPhases(accessToken),listUnitTypes(accessToken),listProjectAssets(accessToken,projectId)]);setPhases(p.phases.filter(x=>x.project_id===projectId));setTypes(t.unit_types.filter(x=>x.project_id===projectId));setAssets(a.assets);},[accessToken,projectId]);useEffect(()=>{void load();},[load]);return <Card className="p-8"><div className="mb-5 flex items-center justify-between"><h2 className="text-base font-semibold">مخزون المشروع</h2>{canManage&&<Button onClick={()=>setShowAssetForm(v=>!v)}>{showAssetForm?'إغلاق':'إضافة عقار'}</Button>}</div>{showAssetForm&&<div className="mb-6"><CreateAssetForm accessToken={accessToken} projectId={projectId} onCreated={async()=>{setShowAssetForm(false);await load();}}/></div>}<div className="grid gap-6 md:grid-cols-3"><section><h3 className="mb-2 font-medium">المراحل <span className="text-text-secondary">({phases.length})</span></h3>{canManage&&<div className="flex gap-2"><Input value={phaseName} onChange={e=>setPhaseName(e.target.value)} placeholder="اسم المرحلة"/><Button onClick={async()=>{if(!phaseName.trim())return;await createPhase(accessToken,{project_id:projectId,name_ar:phaseName});setPhaseName('');await load();}}>إضافة</Button></div>}</section><section><h3 className="mb-2 font-medium">أنواع الوحدات <span className="text-text-secondary">({types.length})</span></h3>{canManage&&<div className="flex gap-2"><Input value={typeName} onChange={e=>setTypeName(e.target.value)} placeholder="اسم النوع"/><Button disabled={!typeName.trim()} onClick={async()=>{await createUnitType(accessToken,{project_id:projectId,name_ar:typeName,asset_type:'apartment',area_sqm:1});setTypeName('');await load();}}>إضافة</Button></div>}</section><section><h3 className="mb-2 font-medium">العقارات <span className="text-text-secondary">({assets.length})</span></h3><p className="text-sm text-text-secondary">الوحدات الفعلية والمباني أصبحت أصولًا موحدة داخل المشروع.</p></section></div>{assets.length>0&&<div className="mt-6 overflow-x-auto"><table className="w-full text-sm"><thead><tr><th className="p-2 text-start">العقار</th><th className="p-2 text-start">النوع</th><th className="p-2 text-start">الحالة</th></tr></thead><tbody>{assets.map(a=><tr key={a.id} className="border-t border-border-subtle"><td className="p-2">{a.name_ar}</td><td className="p-2">{a.asset_type}</td><td className="p-2">{a.physical_status}</td></tr>)}</tbody></table></div>}</Card>}