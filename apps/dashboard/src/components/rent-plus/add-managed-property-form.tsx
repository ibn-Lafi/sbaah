'use client';
import{useEffect,useState}from'react';import{FormWizard,WizardActions}from'@/components/forms/form-wizard';import{Input}from'@/components/ui/input';import{Select}from'@/components/ui/select';import{createManagementAssignment,listAssetOptions,type AssetOption}from'@/lib/api/rent-plus';
export function AddManagedPropertyForm({accessToken,onCreated}:{accessToken:string;onCreated:()=>void}){const[assets,setAssets]=useState<AssetOption[]>([]),[assetId,setAssetId]=useState(''),[startsAt,setStartsAt]=useState(''),[feeType,setFeeType]=useState(''),[feeValue,setFeeValue]=useState(''),[step,setStep]=useState(0),[busy,setBusy]=useState(false),[error,setError]=useState('');useEffect(()=>{void listAssetOptions(accessToken).then(r=>setAssets(r.assets))},[accessToken]);function next(){if(!assetId){setError('اختر العقار للمتابعة');return}setError('');setStep(1)}async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{await createManagementAssignment(accessToken,{asset_id:assetId,starts_at:startsAt||undefined,management_fee_type:feeType||null,management_fee_value:feeValue?Number(feeValue):null});onCreated()}catch(x){setError(x instanceof Error?x.message:'تعذر إضافة العقار للإدارة')}finally{setBusy(false)}}return <form onSubmit={submit} className="flex flex-col gap-5"><FormWizard steps={['العقار وبداية الإدارة','الرسوم والمراجعة']} current={step} onStepChange={target=>target<step&&setStep(target)}/>{step===0&&<div className="grid gap-4 sm:grid-cols-2"><Select value={assetId} onChange={e=>setAssetId(e.target.value)} required><option value="">اختر العقار</option>{assets.map(x=><option key={x.id} value={x.id}>{x.name_ar}</option>)}</Select><Input value={startsAt} onChange={e=>setStartsAt(e.target.value)} type="date"/></div>}{step===1&&<div className="space-y-4"><div className="grid gap-4 sm:grid-cols-2"><Select value={feeType} onChange={e=>setFeeType(e.target.value)}><option value="">بدون رسوم إدارة</option><option value="fixed">مبلغ ثابت</option><option value="percentage">نسبة مئوية</option></Select><Input value={feeValue} onChange={e=>setFeeValue(e.target.value)} type="number" min="0" step="0.01" placeholder="قيمة رسوم الإدارة" disabled={!feeType}/></div><p className="rounded-xl border border-border-default p-4 text-sm">العقار: <strong>{assets.find(x=>x.id===assetId)?.name_ar}</strong></p></div>}{error&&<p className="text-sm text-red-600">{error}</p>}<WizardActions step={step} total={2} loading={busy} submitLabel="إضافة العقار للإدارة" onBack={()=>setStep(0)} onNext={next}/></form>}
