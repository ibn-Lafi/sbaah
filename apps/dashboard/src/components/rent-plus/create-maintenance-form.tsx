'use client';import{useEffect,useState}from'react';import{Button}from'@/components/ui/button';import{Input}from'@/components/ui/input';import{Select}from'@/components/ui/select';import{createMaintenance,listAssetOptions,type AssetOption}from'@/lib/api/rent-plus';export function CreateMaintenanceForm({accessToken,onCreated}:{accessToken:string;onCreated:()=>void}){const[a,setA]=useState<AssetOption[]>([]);useEffect(()=>{void listAssetOptions(accessToken).then(r=>setA(r.assets))},[accessToken]);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await createMaintenance(accessToken,{request_number:f.get('request_number'),asset_id:f.get('asset_id'),title:f.get('title'),category:f.get('category')||null,description:f.get('description')||null,priority:f.get('priority')});onCreated()}return <form onSubmit={submit} className="space-y-4"><Input name="request_number" placeholder="رقم الطلب" required/><Select name="asset_id" className="w-full" required><option value="">اختر العقار</option>{a.map(x=><option key={x.id} value={x.id}>{x.name_ar}</option>)}</Select><Input name="title" placeholder="عنوان المشكلة" required/><Input name="category" placeholder="التصنيف"/><Input name="description" placeholder="وصف المشكلة"/><Select name="priority" className="w-full"><option value="normal">عادية</option><option value="low">منخفضة</option><option value="high">عالية</option><option value="urgent">عاجلة</option></Select><Button type="submit">إنشاء طلب الصيانة</Button></form>}