'use client';
import { useEffect,useState } from 'react'; import { Button } from '@/components/ui/button'; import { Modal } from '@/components/ui/modal'; import { AppShell } from '@/components/layout/app-shell'; import { Card } from '@/components/ui/card'; import { TableSkeleton } from '@/components/ui/table-skeleton'; import { useCurrentUser } from '@/lib/auth/current-user-context'; import { listManagedProperties, type ManagedPropertyRow } from '@/lib/api/rent-plus'; import { AddManagedPropertyForm } from '@/components/rent-plus/add-managed-property-form';
export default function Page(){const{me,accessToken}=useCurrentUser();const[data,setData]=useState<ManagedPropertyRow[]|null>(null);const[showCreate,setShowCreate]=useState(false);useEffect(()=>{let active=true;void listManagedProperties(accessToken).then(r=>{if(active)setData(r.properties)});return()=>{active=false}},[accessToken]);return <AppShell title="العقار" orgName={me.tenant.name_ar} accountType={me.tenant.account_type}><div className="mb-5 flex justify-end"><Button onClick={()=>setShowCreate(true)}>إضافة عقار</Button></div>{showCreate&&<Modal title="إضافة عقار" onClose={()=>setShowCreate(false)}><AddManagedPropertyForm accessToken={accessToken} onCreated={()=>{setShowCreate(false);location.reload()}}/></Modal>}<Card className="overflow-hidden">{data===null?<TableSkeleton columns={4}/>:data.length===0?<p className="text-text-secondary p-8 text-center">لا توجد بيانات حتى الآن</p>:<div className="overflow-x-auto"><table className="w-full min-w-[640px] text-sm"><thead className="bg-surface-header text-text-secondary text-right"><tr><th className="px-4 py-3 font-medium">العقار</th><th className="px-4 py-3 font-medium">المرجع</th><th className="px-4 py-3 font-medium">الحالة</th><th className="px-4 py-3 font-medium">بداية الإدارة</th></tr></thead><tbody>{data.map((row:ManagedPropertyRow)=><tr key={row.id} className="border-border-subtle border-t"><td className="px-4 py-3">{row.assets?.name_ar ?? '—'}</td><td className="px-4 py-3">{row.assets?.reference_number ?? '—'}</td><td className="px-4 py-3">{row.status}</td><td className="px-4 py-3">{row.starts_at}</td></tr>)}</tbody></table></div>}</Card></AppShell>}