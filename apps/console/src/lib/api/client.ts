/** Re-exported from @sbaah/shared (code-quality audit finding — this was a near-verbatim duplicate across dashboard/console/public-site). Kept as its own file so every existing `@/lib/api/client` import in this app stays unchanged. */
export { ApiRequestError, apiGet, apiPost, apiPatch, apiDelete } from '@sbaah/shared';
