import type{SupabaseClient}from'@supabase/supabase-js';export async function handoffConversation(s:SupabaseClient,tenantId:string,conversationId:string,assignedUserId?:string|null){const{data,error}=await s.from('whatsapp_conversations').update({status:'human_handoff',assigned_user_id:assignedUserId??null}).eq('tenant_id',tenantId).eq('id',conversationId).select().single();if(error)throw new Error(error.message);return data;}