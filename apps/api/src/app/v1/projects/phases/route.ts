import type { NextRequest } from 'next/server'; import { phaseInputSchema } from '@sbaah/shared'; import { okResponse,withErrorHandling } from '@/lib/http'; import { getAuthenticatedClient } from '@/lib/auth/get-authenticated-client'; import { getCallerContext } from '@/lib/auth/get-caller-context'; import { assertPermission } from '@/lib/auth/permissions'; import { assertTenantOwnedRow } from '@/lib/tenant/assert-tenant-owned-row';
export const GET=withErrorHandling(async(r:NextRequest)=>{const{supabase}=getAuthenticatedClient(r);const c=await getCallerContext(supabase);assertPermission(c.role,'projects.read');const{data,error}=await supabase.from('project_phases').select('*').eq('tenant_id',c.tenantId).order('order_index');if(error)throw new Error(error.message);return okResponse({phases:data??[]});});
export const POST=withErrorHandling(async(r:NextRequest)=>{const{supabase}=getAuthenticatedClient(r);const c=await getCallerContext(supabase);assertPermission(c.role,'projects.create');const i=phaseInputSchema.parse(await r.json());await assertTenantOwnedRow({supabase,table:'projects',id:i.project_id,tenantId:c.tenantId,label:'المشروع'});const{data,error}=await supabase.from('project_phases').insert({...i,tenant_id:c.tenantId}).select().single();if(error)throw new Error(error.message);return okResponse({phase:data},201);});