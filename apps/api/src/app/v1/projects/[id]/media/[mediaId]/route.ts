import { z } from 'zod';
import { ApiError, okResponse, withErrorHandling } from '@/lib/http';
import { getAuthenticatedClient } from '@/lib/auth/get-authenticated-client';
import { getCallerContext } from '@/lib/auth/get-caller-context';
import { assertTenantOwnedRow } from '@/lib/tenant/assert-tenant-owned-row';
const categories=['general','exterior','master_plan','unit_plans','interior','amenities','location','construction','other'] as const;
const schema=z.object({category:z.enum(categories).optional(),alt_ar:z.string().max(200).nullable().optional(),alt_en:z.string().max(200).nullable().optional(),order_index:z.number().int().nonnegative().optional(),is_primary:z.boolean().optional()}).refine(v=>Object.keys(v).length>0);
export const PATCH=withErrorHandling(async(request,{params}:{params:Promise<{id:string;mediaId:string}>})=>{const{id,mediaId}=await params;const{supabase}=getAuthenticatedClient(request);const c=await getCallerContext(supabase);if(c.role==='agent')throw new ApiError(403,'forbidden','لا يملك الوسيط صلاحية إدارة وسائط المشروع');await assertTenantOwnedRow({supabase,table:'projects',id,tenantId:c.tenantId,label:'المشروع'});const input=schema.parse(await request.json());if(input.is_primary){const{error}=await supabase.from('project_media').update({is_primary:false}).eq('tenant_id',c.tenantId).eq('project_id',id).eq('is_primary',true);if(error)throw new Error(error.message);}const{data,error}=await supabase.from('project_media').update(input).eq('id',mediaId).eq('project_id',id).eq('tenant_id',c.tenantId).select().maybeSingle();if(error)throw new Error(error.message);if(!data)throw new ApiError(404,'project_media_not_found','الوسائط غير موجودة');return okResponse({media:data});});
export const DELETE=withErrorHandling(async(request,{params}:{params:Promise<{id:string;mediaId:string}>})=>{const{id,mediaId}=await params;const{supabase}=getAuthenticatedClient(request);const c=await getCallerContext(supabase);if(c.role==='agent')throw new ApiError(403,'forbidden','لا يملك الوسيط صلاحية إدارة وسائط المشروع');await assertTenantOwnedRow({supabase,table:'projects',id,tenantId:c.tenantId,label:'المشروع'});const{data,error}=await supabase.from('project_media').delete().eq('id',mediaId).eq('project_id',id).eq('tenant_id',c.tenantId).select('id').maybeSingle();if(error)throw new Error(error.message);if(!data)throw new ApiError(404,'project_media_not_found','الوسائط غير موجودة');return okResponse({status:'deleted'});});