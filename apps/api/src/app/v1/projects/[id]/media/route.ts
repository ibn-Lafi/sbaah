import type{NextRequest}from'next/server';import{z}from'zod';import{okResponse,withErrorHandling}from'@/lib/http';import{getAuthenticatedClient}from'@/lib/auth/get-authenticated-client';import{getCallerContext}from'@/lib/auth/get-caller-context';import{assertPermission}from'@/lib/auth/permissions';import{assertTenantOwnedRow}from'@/lib/tenant/assert-tenant-owned-row';const schema=z.object({url:z.string().url(),media_type:z.enum(['image','video']).default('image'),alt_ar:z.string().max(200).optional().nullable(),alt_en:z.string().max(200).optional().nullable(),order_index:z.number().int().min(0).default(0)});
export const GET=withErrorHandling(async(r:NextRequest,cx:{params:Promise<{id:string}>})=>{const{id}=await cx.params,{supabase}=getAuthenticatedClient(r),c=await getCallerContext(supabase);assertPermission(c.role,'projects.read');await assertTenantOwnedRow({supabase,table:'projects',id,tenantId:c.tenantId,label:'المشروع'});const{data,error}=await supabase.from('project_media').select('*').eq('tenant_id',c.tenantId).eq('project_id',id).order('order_index');if(error)throw new Error(error.message);return okResponse({media:data??[]});});
export const POST=withErrorHandling(async(r:NextRequest,cx:{params:Promise<{id:string}>})=>{const{id}=await cx.params,{supabase}=getAuthenticatedClient(r),c=await getCallerContext(supabase);assertPermission(c.role,'projects.create');await assertTenantOwnedRow({supabase,table:'projects',id,tenantId:c.tenantId,label:'المشروع'});const i=schema.parse(await r.json());const{data,error}=await supabase.from('project_media').insert({...i,tenant_id:c.tenantId,project_id:id}).select().single();if(error)throw new Error(error.message);return okResponse({media:data},201);});