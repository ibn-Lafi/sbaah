import type { NextRequest } from 'next/server'; import { leaseInstallmentInputSchema } from '@sbaah/shared'; import { okResponse,withErrorHandling } from '@/lib/http'; import { getAuthenticatedClient } from '@/lib/auth/get-authenticated-client'; import { getCallerContext } from '@/lib/auth/get-caller-context';
export const GET=withErrorHandling(async(request:NextRequest)=>{const{supabase}=getAuthenticatedClient(request);let q=supabase.from('lease_installments').select('*').order('due_date');const id=request.nextUrl.searchParams.get('contract_id');if(id)q=q.eq('contract_id',id);const{data,error}=await q;if(error)throw new Error(`Failed to list installments: ${error.message}`);return okResponse({installments:data});});
export const POST=withErrorHandling(async(request:NextRequest)=>{const{supabase}=getAuthenticatedClient(request);const caller=await getCallerContext(supabase);const input=leaseInstallmentInputSchema.parse(await request.json());const{data,error}=await supabase.from('lease_installments').insert({...input,tenant_id:caller.tenantId}).select().single();if(error)throw new Error(`Failed to create installment: ${error.message}`);return okResponse({installment:data},201);});