import type { NextRequest } from 'next/server';import {leadRequirementSchema} from '@sbaah/shared';import{okResponse,withErrorHandling}from'@/lib/http';import{getAuthenticatedClient}from'@/lib/auth/get-authenticated-client';import{getCallerContext}from'@/lib/auth/get-caller-context';import{assertPermission}from'@/lib/auth/permissions';import{assertTenantOwnedRow}from'@/lib/tenant/assert-tenant-owned-row';
export const GET=withErrorHandling(async(r:NextRequest)=>{const{supabase}=getAuthenticatedClient(r),c=await getCallerContext(supabase);assertPermission(c.role,'crm.read');const leadId=r.nextUrl.searchParams.get('lead_id');let q=supabase.from('lead_requirements').select('*').eq('tenant_id',c.tenantId);if(leadId)q=q.eq('lead_id',leadId);const{data,error}=await q.order('created_at',{ascending:false});if(error)throw new Error(error.message);return okResponse({requirements:data??[]});});
export const POST=withErrorHandling(async(r:NextRequest)=>{const{supabase}=getAuthenticatedClient(r),c=await getCallerContext(supabase);assertPermission(c.role,'crm.create');const b=await r.json(),leadId=String(b.lead_id??'');await assertTenantOwnedRow({supabase,table:'leads',id:leadId,tenantId:c.tenantId,label:'العميل'});const i=leadRequirementSchema.parse(b);const{data,error}=await supabase.from('lead_requirements').insert({...i,lead_id:leadId,tenant_id:c.tenantId}).select().single();if(error)throw new Error(error.message);return okResponse({requirement:data},201);});