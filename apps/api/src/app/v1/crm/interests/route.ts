import type{NextRequest}from'next/server';import{z}from'zod';import{okResponse,withErrorHandling}from'@/lib/http';import{getAuthenticatedClient}from'@/lib/auth/get-authenticated-client';import{getCallerContext}from'@/lib/auth/get-caller-context';import{assertPermission}from'@/lib/auth/permissions';import{assertOptionalTenantOwnedRow,assertTenantOwnedRow}from'@/lib/tenant/assert-tenant-owned-row';const schema=z.object({lead_id:z.string().uuid(),property_id:z.string().uuid().optional().nullable(),unit_id:z.string().uuid().optional().nullable()}).refine(v=>Number(!!v.property_id)+Number(!!v.unit_id)===1);
export const GET=withErrorHandling(async(r:NextRequest)=>{const{supabase}=getAuthenticatedClient(r),c=await getCallerContext(supabase);assertPermission(c.role,'crm.read');const leadId=r.nextUrl.searchParams.get('lead_id');let q=supabase.from('lead_interests').select('*').eq('tenant_id',c.tenantId);if(leadId)q=q.eq('lead_id',leadId);const{data,error}=await q.order('created_at',{ascending:false});if(error)throw new Error(error.message);return okResponse({interests:data??[]});});
export const POST=withErrorHandling(async(r:NextRequest)=>{const{supabase}=getAuthenticatedClient(r),c=await getCallerContext(supabase);assertPermission(c.role,'crm.create');const i=schema.parse(await r.json());await assertTenantOwnedRow({supabase,table:'leads',id:i.lead_id,tenantId:c.tenantId,label:'العميل'});await assertOptionalTenantOwnedRow({supabase,table:'properties',id:i.property_id,tenantId:c.tenantId,label:'العقار'});await assertOptionalTenantOwnedRow({supabase,table:'units',id:i.unit_id,tenantId:c.tenantId,label:'الوحدة'});const{data,error}=await supabase.from('lead_interests').insert({...i,tenant_id:c.tenantId}).select().single();if(error)throw new Error(error.message);return okResponse({interest:data},201);});