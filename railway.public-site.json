{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter=@sbaah/public-site build",
    "watchPatterns": ["apps/public-site/**", "packages/shared/**"]
  },
  "deploy": {
    "startCommand": "pnpm --filter=@sbaah/public-site exec next start -p $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
