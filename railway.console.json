{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter=@sbaah/console build",
    "watchPatterns": ["apps/console/**", "packages/shared/**"]
  },
  "deploy": {
    "startCommand": "pnpm --filter=@sbaah/console exec next start -p $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
