-- =============================================================================
-- Migration 0049: website.banner_video_url
--
-- قسم الهيرو (ثيم الأساسي) صار له 4 أشكال (HeroSectionConfig.variant،
-- packages/shared): صورة فقط / صورة مع فلتر بحث العقارات / فيديو فقط /
-- فيديو مع فلتر بحث العقارات. الصورة (banner_image_url) كانت موجودة
-- أصلًا؛ هذا العمود يضيف مكافئه للفيديو — بديل عنه لا يظهران معًا،
-- يُحدَّد أيهما يُستخدم فعليًا عبر `variant` بإعدادات كل قسم Hero.
-- =============================================================================

alter table websites add column if not exists banner_video_url text;
